# 需求实施计划

- [ ] 1. 重构应用骨架与导航框架
  - 在 `src/App.tsx`、`src/components/Navbar.tsx`、`src/pages/*` 中重构整体布局，建立“顶部操作栏 + 左侧四步导航 + 右侧主工作区”框架，对应设计文档第 5 节
  - 新增四步页面壳层：`视频设置`、`剧本编辑`、`资产库`、`分镜脚本`，并为每一步预留空状态、进行态和错误态区域，对应设计文档第 5.2、5.3 节
  - 将现有 `Workspace.tsx` 中的五段式流程能力拆出并重新映射到四步结构，避免继续沿用单页堆叠布局，对应设计文档第 2 节目标 1、第 8 节

- [ ] 2. 实现本地配置层与项目库基础模型
  - [ ] 2.1 在 `src/types/` 中新增全局配置、项目、分集、资产、导出记录、模板索引等数据类型，覆盖设计文档第 7.1、7.2 节
  - [ ] 2.2 在 `src/services/` 或新增 `src/store/` 中实现配置读写接口，支持全局默认参数、输出目录、账号信息、界面偏好的本地持久化，对应设计文档第 7.1 节
  - [ ] 2.3 实现应用内项目库读写接口，支持项目列表、项目详情、项目级参数覆盖、分集结果和资产引用持久化，对应设计文档第 3.2、7.2 节
  - [ ]* 2.4 为配置模型和项目模型编写单元测试，校验字段序列化、默认值回退和非法数据容错，对应设计文档第 10 节第 2、4 点

- [ ] 3. 建立提示词文件系统与模板目录机制
  - [ ] 3.1 设计默认模板目录和用户模板目录的数据结构，定义六类模板文件的命名、索引和读取优先级，对应设计文档第 3.4、6.1、7.4 节
  - [ ] 3.2 实现模板文件加载、保存、恢复默认、缺失回退和错误上报逻辑，确保模板按文件处理而不是只保存在配置字段中，对应设计文档第 6.1、6.2、9.2 节
  - [ ] 3.3 在 `electron/preload.ts` 和 `electron/main.ts` 中增加模板文件读写所需的安全 IPC 接口，支持前端读写本地模板目录，对应设计文档第 6 节、第 7 节
  - [ ]* 3.4 为模板文件加载与回退机制编写单元测试和集成测试，覆盖文件缺失、编码异常和用户覆盖文件场景，对应设计文档第 9.2、10.1 节

- [ ] 4. 检查点 - 确保所有测试通过,如有疑问请询问用户
  - 校验应用骨架、本地配置层和模板文件系统可以稳定运行
  - 确认数据模型和 IPC 接口没有阻塞后续页面开发的问题

- [ ] 5. 重构视频设置页与参数继承逻辑
  - [ ] 5.1 在新的 `视频设置` 页面中实现风格、比例、质量、平台、时长和关键词预览区域，风格与比例采用已确认预设，对应设计文档第 3.5、5.3 节
  - [ ] 5.2 实现“全局默认 + 项目覆盖”的参数继承逻辑，保证项目优先、全局兜底，对应设计文档第 3.5、6.4、8.1 节
  - [ ] 5.3 实现自动保存和顶部显式保存联动，编辑后自动落盘，顶部保存用于显式反馈，对应设计文档第 3.2、9.1 节
  - [ ]* 5.4 为参数继承与自动保存逻辑编写单元测试，覆盖全局默认值、项目覆盖值和清空覆盖值后的回退行为，对应设计文档第 10 节第 2、4 点

- [ ] 6. 实现提示词管理界面与结构化编辑能力
  - [ ] 6.1 新增提示词管理入口与弹窗或独立工作区，构建左侧六类模板列表和右侧编辑区，对应设计文档第 5.4、6.1 节
  - [ ] 6.2 在编辑器中实现标题识别、变量高亮、规则段识别、示例段识别和约束段识别，保持“文件内容优先”的编辑原则，对应设计文档第 3.4、6.3 节
  - [ ] 6.3 增加模板保存、恢复默认、模板脏状态提示和模板预览能力，确保用户可维护大文本模板，对应设计文档第 6.2、6.3、9.2 节
  - [ ]* 6.4 为模板编辑器编写组件测试，覆盖变量高亮、分段识别和恢复默认交互，对应设计文档第 10 节第 1 点

- [ ] 7. 重构剧本编辑页与分集生成链路
  - [ ] 7.1 在 `剧本编辑` 页面实现大纲基础信息区，包括标题、主题、简介和导入入口，并接入自动保存，对应设计文档第 5.3、8 节
  - [ ] 7.2 实现基于大纲的一键生成分集流程，重构现有 Coze 调用参数与结果承接逻辑，对应设计文档第 3.6、8 节第 1 到 3 步
  - [ ] 7.3 构建分集列表区、空状态、展开查看和错误回退机制，支持后续资产提取和分镜提示词生成，对应设计文档第 5.3、9.3 节
  - [ ]* 7.4 为分集生成结果解析编写单元测试，覆盖成功返回、空返回和结构异常返回，对应设计文档第 9.3、10 节第 5 点

- [ ] 8. 实现资产提取与资产库页面
  - [ ] 8.1 新建 `资产库` 页面与资产数据结构承接逻辑，支持角色、场景、道具三类资产展示，对应设计文档第 3.6、5.3 节
  - [ ] 8.2 实现从分集内容触发资产提取的流程，接入角色提取、场景提取、资产生成等模板文件，对应设计文档第 3.6、6.4、8 节第 4、5 步
  - [ ] 8.3 实现资产确认、修正、入库与引用逻辑，为分镜脚本页提供稳定输入，对应设计文档第 5.3、7.2、8 节
  - [ ]* 8.4 为资产提取结果解析与入库逻辑编写单元测试，覆盖 JSON 解析失败、字段缺失和人工修正保存，对应设计文档第 9.3、10 节第 5 点

- [ ] 9. 检查点 - 确保所有测试通过,如有疑问请询问用户
  - 校验视频设置、提示词管理、剧本编辑和资产提取四个核心模块可以联通
  - 确认从大纲到分集再到资产库的链路已经走通

- [ ] 10. 实现分镜脚本页与提示词拼装引擎
  - [ ] 10.1 新建 `分镜脚本` 页面，提供分镜提示词生成区、原始文本编辑区和导出入口，对应设计文档第 3.6、5.3 节
  - [ ] 10.2 实现提示词拼装引擎，按“系统提示词文件 + 分镜后缀文件 + 项目参数 + 分集内容 + 资产引用”组合最终执行文本，对应设计文档第 6.4、8.2 节
  - [ ] 10.3 实现变量解析与替换逻辑，覆盖 `{style}`、`{aspectRatio}`、`{quality}`、`{projectName}`、`{episodeTitle}`、`{seconds}`、`{platform}`、`{character}`、`{scene}`、`{prop}`、`{shotType}`、`{cameraMovement}`，对应设计文档第 3.4、6.4 节
  - [ ]* 10.4 为提示词拼装与变量替换编写单元测试和性质测试，验证必填变量替换完整、项目覆盖优先和模板拼装顺序正确，对应设计文档第 10 节第 1、2、5 点

- [ ] 11. 实现导出链路与输出目录落盘机制
  - [ ] 11.1 在 `electron/main.ts` 和 `electron/preload.ts` 中增加输出目录选择、目录校验和文件导出 IPC 接口，对应设计文档第 3.2、7.3、9.4 节
  - [ ] 11.2 实现按 `输出根目录/项目名/script|assets|storyboard|exports` 的落盘逻辑，确保脚本文本、资产数据、分镜结果和完整导出包进入对应目录，对应设计文档第 7.3、8 节第 7 步
  - [ ] 11.3 实现两类导出：导出分镜提示词与脚本文本、导出项目完整资料包，并处理目录不可写、文件名冲突和非法字符，对应设计文档第 3.6、9.4 节
  - [ ]* 11.4 为导出目录构造和安全文件名处理编写单元测试，覆盖项目名冲突、空目录和非法路径字符，对应设计文档第 9.4、10 节第 3 点

- [ ] 12. 实现安装、品牌视觉与桌面端收尾
  - [ ] 12.1 调整 `package.json` 中的 `electron-builder` 配置，开启 NSIS 自定义安装路径能力并验证现有发布流程兼容性，对应设计文档第 3.1 节
  - [ ] 12.2 接入新的应用图标、界面左上角 Logo 和静态启动页资源，覆盖应用程序图标、任务栏图标和安装包图标，对应设计文档第 3.1、2 节目标 8
  - [ ] 12.3 清理旧的五段式界面残留、统一四步流程文案和空状态提示，完成桌面端交互收尾，对应设计文档第 5 节、第 13 节
  - [ ]* 12.4 为安装配置和视觉资源接入补充回归检查清单或自动化校验脚本，对应设计文档第 10 节第 4 点

- [ ] 13. 最终检查点 - 确保所有测试通过,如有疑问请询问用户
  - 确认四步工作流、提示词文件体系、本地配置层、项目库和导出链路全部可用
  - 确认首版范围内的异常处理、自动保存和桌面端配置行为符合设计文档要求
